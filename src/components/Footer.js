import "../styles/Footer.module.css"
export default function Footer(){
    return(
        <footer>
            <p>As a general rule, the most successful man in life 
               is the man who has the best information.
            </p>
        </footer>
    );
}